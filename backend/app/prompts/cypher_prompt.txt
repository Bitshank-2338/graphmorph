Generate a Cypher query for a Neo4j graph with this schema:

Node labels: Person, Department, Skill, Customer, Product, Category, Region, Entity
Common relationships: BELONGS_TO, HAS_SKILL, WORKS_WITH, PURCHASED, IN_CATEGORY, IN_REGION

Rules:
- Return ONLY the Cypher query, nothing else.
- Limit results to <=100 rows.
- Prefer aggregations and ordering when the question asks for ranking.
- Never use APOC procedures.
